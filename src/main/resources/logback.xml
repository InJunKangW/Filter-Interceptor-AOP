<configuration>

    <!-- 콘솔 로그 출력 설정 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 루트 로거 설정 (error 레벨 이상의 로그만 출력) -->
    <root level="error">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
